===============================================================================
БЛОК-СХЕМА: БЕЗОПАСНЫЙ ПОТОК ДАННЫХ В GRAPH_RAG (С ЭКРАНОМ ЗАЩИТЫ И NEO4J)
===============================================================================

[ Пользователь: Студент / Преподаватель ]
                  │
                  │ (1) Отправляет вопрос (Текст) + Токен (JWT)
                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                 РУБЕЖ 1: ВХОДНОЙ ЭКРАН ЗАЩИТЫ (MIDDLEWARE)                  │
├─────────────────────────────────────────────────────────────────────────────┤
│ Шаг 1. Декодирование токена JWT. Извлечение ролей пользователя.            │
│       Пример: "ivanov_i" -> Роли: ["student", "group_iu7"]                  │
│                                                                             │
│ Шаг 2. Санитизация ввода (Проверка на Prompt Injection / Jailbreak).        │
└─────────────────────────────────────────────────────────────────────────────┘
                  │
                  ├─► [ЕСТЬ УГРОЗА / ВЗЛОМ?] ──► ДА ──► [ Блокировка запроса ]
                  │                                     (Вывод ошибки 403)
                  ▼ НЕТ
┌─────────────────────────────────────────────────────────────────────────────┐
│                  РУБЕЖ 2: ИДЕМПОТЕНТНЫЙ ПОИСКОВЫЙ ДВИЖОК                    │
├─────────────────────────────────────────────────────────────────────────────┤
│ Принимает параметры:                                                        │
│  - query_text: "Когда пересдача по матану?"                                 │
│  - user_roles: ["student", "group_iu7"]                                     │
└─────────────────────────────────────────────────────────────────────────────┘
                  │
                  │ (2) Генерация Cypher-запроса с параметрами безопасности
                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       БАЗА ДАННЫХ (NEO4J GRAPH DB)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│ Выполняется внутренний гибридный поиск:                                     │
│  1. Vector Index Match (Векторное сходство чанков)                          │
│  2. Full-text Index Match (Полнотекстовый поиск ключевых слов)              │
│                                                                             │
│ [ КРИТИЧЕСКИЙ ФИЛЬТР БЕЗОПАСНОСТИ (PRE-FILTERING) ]                         │
│ WHERE any(role IN chunk.allowed_roles WHERE role IN $user_roles)            │
│ -> База данных физически отсекает чанки документов других отделов/кафедр    │
│                                                                             │
│  3. Слияние весов (RRF) -> Выборка ТОП-5 разрешенных чанков                 │
└─────────────────────────────────────────────────────────────────────────────┘
                  │
                  │ (3) Возвращает только разрешенный контекст (Текст чанков)
                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│              ОРКЕСТРАТОР: ГЕНЕРАЦИЯ И СБОРКА ПРОМПТА ДЛЯ LLM                │
├─────────────────────────────────────────────────────────────────────────────┤
│ Формируется безопасный Prompt:                                              │
│ "Ответь на вопрос пользователя, используя только этот контекст: <Чанки>"    │
└─────────────────────────────────────────────────────────────────────────────┘
                  │
                  │ (4) Отправка промпта в нейросеть
                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            МОДЕЛЬ ЯЗЫКА (LLM)                               │
├─────────────────────────────────────────────────────────────────────────────┤
│ Генерирует ответ. Модель физически изолирована от закрытых данных,          │
│ так как они не были переданы в составе промпта на предыдущем шаге.          │
└─────────────────────────────────────────────────────────────────────────────┘
                  │
                  │ (5) Возвращает сырой сгенерированный текст ответа
                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                 РУБЕЖ 3: ВЫХОДНОЙ ЭКРАН ЗАЩИТЫ (MIDDLEWARE)                 │
├─────────────────────────────────────────────────────────────────────────────┤
│ Валидация сгенерированного ответа:                                          │
│  - Проверка по регулярным выражениям (Regex) на утечку персональных         │
│    данных (PII): номера телефонов, паспорта, зашифрованные ID.              │
└─────────────────────────────────────────────────────────────────────────────┘
                  │
                  ├─► [ОБНАРУЖЕНА УТЕЧКА PII?] ──► ДА ──► [ Маскирование /    ]
                  │                                       [ Замена на шаблоны ]
                  ▼ НЕТ
┌─────────────────────────────────────────────────────────────────────────────┐
│                           ФИНАЛЬНЫЙ БЕЗОПАСНЫЙ ОТВЕТ                        │
├─────────────────────────────────────────────────────────────────────────────┤
│ Выводится пользователю в чат-интерфейс.                                     │
└─────────────────────────────────────────────────────────────────────────────┘

===============================================================================
Обозначения потоков:
──►  - Успешный безопасный сценарий выполнения (Happy Path)
──► ДА - Сценарий обнаружения угрозы (Срабатывание триггеров безопасности)
===============================================================================
