# Review-record: Вариант А, typed-ontology контракт

```text
review-record:
  proposal: >
    Изначально Dz4/proposals.md. Перенесён в
    docs/archive/superseded_proposals/proposals_typed_ontology_variant_a.md.
  reviewer/date: sol_pro, 2026-09-25 (superseded), ревью-регистрация 2026-09-26
  decision: superseded
  accepted_scope: >
    Ничего в прежнем виде. Ценность сохранилась как диагноз: причиной варнингов
    Neo4j на M5 было то, что ExtractStage писал все сущности одной меткой Entity
    без типизации из YAML-профиля, а рёбра строились по сырому name.
  rejected_scope: >
    Контракт, целиком построенный вокруг типизированной онтологии, как нормативная
    цель: он требовал, чтобы граф был основной осью, что противоречит принятой
    vector-first норме.
  evidence: >
    openspec/changes/add-lightweight-context-graph/ — текущий target: vector-only
    baseline и optional graph experiment; openspec/changes/eval-graph-contribution-experiment/
    tasks.md §8 — реализация узлов и рёбер перенесена туда как DoD 8.1-8.3.
  assumptions: >
    Автор исходного текста считал, что онтология — основа контура. Это допущение
    не выдержало проверки paired-экспериментом: выигрыш графа измеряется, а не
    предполагается.
  risks: >
    Документ был помечен «не исполнять как DoD», но лежал в корне проекта рядом с
    Proposals/, то есть выглядел как актуальная спецификация.
  follow_up_change: >
    Не требуется: принятая часть перенесена в add-lightweight-context-graph и
    eval-graph-contribution-experiment.
  revisit_when: >
    Не возвращается. Если типизированная онтология снова потребуется как основа
    контура, это новое предложение в Proposals/ с новым обоснованием.
```

**Суть.** Единственное предложение, имевшее собственный вердикт `superseded`: автор
уже 2026-09-25 пометил его как неактуальное, но до регистрации в бандле оно продолжало
лежать в корне проекта.

**Побочная находка.** Раздел «Исследованные материалы» ссылался на пути, которых нет
в репозитории: `docs/archive/v1_skeleton/`, `docs/archive/README.md`,
`ontology_matrix.csv`, а также на шесть доменных профилей вместо трёх. Файл перенесён
в архив без правки текста, но с примечанием о состоянии ссылок — иначе архивная копия
уводила бы читателя по несуществующим путям.

**Отдельно про `ontology_matrix.csv`.** Документ называл его «справочником инвариантов
связки чанков, не замена YAML», то есть считал рабочей ценностью. Файл удалён как
черновик после проверки ссылок: сам по себе он не использовался, битых ссылок на него
в коде нет, а YAML-профиль `it` уже содержит итог. Решение спорное, поэтому фиксирую
явно: если инварианты связки чанков понадобятся как отдельный справочник, их нужно
завести заново из `domain_profile.it.yaml`, а не восстанавливать из этого файла.
