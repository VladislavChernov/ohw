# Python / uv
.venv/
__pycache__/
*.py[co]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Локальные артефакты процесса (не публикуются)
review/
QA_review/
# Ingest/analitic/ больше не игнорируется: аналитика по модулям — целевое
# обоснование будущих решений, в т.ч. ингеста (см. Dz4/analitic/ANALYTICAL_NOTES_RULES.md)

# Секреты локального контура (не публикуются): ключи API и пароль Neo4j
prototype/infra/.env

# Runtime data (SQLite прототипа, локальные прогоны)
prototype/runtime/
prototype/reports/

# Eval pilot: локальный snapshot корпуса и квитанции загрузки (генерируются, не публикуются)
prototype/infra/eval/pilots/docs-review/snapshot/