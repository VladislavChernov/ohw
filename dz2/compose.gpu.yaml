# GPU override: NVIDIA CUDA. Use as:
#   docker compose --profile standalone -f compose.yaml -f compose.gpu.yaml up --build
services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

