# Read only by `docker compose` (app service). The Python app itself does NOT
# read this file.
# App joins the shared infra network `ohw_net` and reaches ollama by its
# container name `ohw-ollama`:
OLLAMA_BASE_URL=http://ohw-ollama:11434
OLLAMA_MODEL=qwen2.5:7b-instruct