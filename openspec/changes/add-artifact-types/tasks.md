# Tasks: типы артефактов и форматы ответа

## 1. Модели и вход

- [x] 1.1 `ArtifactType` (Literal) и `InputDoc.type` в `models.py`
- [x] 1.2 Парсер front-matter в `input_doc.py`: отсутствие → `testcases`,
      корректный `type:`, битый блок/неизвестный тип → `InputError`
- [x] 1.3 Тесты парсера: нет маркеров, есть тип, лишний ключ, неизвестный
      тип, текст с `---` после блока

## 2. Хендлеры типов и промпты

- [x] 2.1 Структура хендлера (`build_prompt`, `validate_response`,
      `render_section`, `wants_json`) и реестр по типам
- [x] 2.2 JSON-хендлер testcases: обёртки над существующими
      prompt/validator/renderer (поведение не меняется)
- [x] 2.3 Markdown-хендлер checklist: промпт с обязательными заголовками
      зон (константа общая с линтом), запрет ограждений кода
- [x] 2.4 Markdown-хендлер testplan: промпт с фиксированными разделами
      (цели/объём/подход/критерии/риски), та же константа в линте

## 3. Линтер markdown

- [x] 3.1 Срез ограждений ```markdown/```; детект JSON-ответа
- [x] 3.2 Проверка обязательных заголовков + непустое содержимое до
      следующего заголовка; issue с именем недостающего/пустого раздела
- [x] 3.3 Проверка пунктов списка под каждой зоной чек-листа (≥1)
- [x] 3.4 Тесты линтера на всех ветках

## 4. Клиент ollama

- [x] 4.1 Параметр запроса из хендлера: `format="json"` только при
      `wants_json`
- [x] 4.2 Тест: markdown-запрос не содержит format=json, JSON-запрос
      содержит (перехват httpx)

## 5. CLI, рендер и склейка

- [x] 5.1 Выбор хендлера по `doc.type`; общий retry-цикл для обоих
      форматов; exit 4 после исчерпания попыток
- [x] 5.2 Рендер отчёта: шапка+сводка с типом; passthrough тела для
      markdown; count только для testcases
- [x] 5.3 Тест CLI: смешанный батч трёх типов → три отчёта нужных
      форматов; битый тип → exit 2 до ollama

## 6. Документация и примеры

- [ ] 6.1 README: front-matter, таблица «тип → формат → что проверяется»
- [ ] 6.2 Прогон интеграционно по всем шести входам `./input` на живой
      модели; примеры отчётов в `examples/output`

## 7. Трассировка бизнес-требований (BR)

- [x] 7.1 Входные файлы `./input`: требования пронумерованы BR-1…BR-7,
      задача явно просит указывать номер в артефакте
- [x] 7.2 `TestCase.requirement: str | None` в схеме; пример поля в
      JSON-схеме промпта; условное MUST-правило в user-промпте (когда во
      входе есть BR-иды)
- [x] 7.3 Рендер кейса печатает «Проверяет требование»; правила для
      markdown: пункт чек-листа завершается «(BR-N)», план перечисляет
      охваченные BR в объёме
- [x] 7.4 Интеграционная проверка трассировки на живой модели
