# Задачи

## 1. Dev Container и документация

- [ ] Добавить `.devcontainer/devcontainer.json` (Python 3.13-bookworm)
- [ ] Завendorить features локально (uv, docker-outside-of-docker) — фикс ghcr.io
- [ ] Написать README.md: описание, быстрый старт, как запустить (simple + advanced)

## 2. Подготовка окружения (общее)

- [ ] Инициализировать проект (pyproject.toml, uv, .gitignore)
- [ ] Создать `.env` с `OLLAMA_BASE_URL=http://host.docker.internal:11434` и `OLLAMA_MODEL`
- [ ] Создать `compose.yaml` (app + shared ollama, standalone-профиль) — по образцу dz2
- [ ] Подключиться к shared ollama из `infra/` (без per-project ollama по умолчанию)
- [ ] Выбрать и скачать модель, подходящую для генерации Python/Pytest/Requests кода

## 3. Парсинг Swagger (общее)

- [ ] Реализовать загрузку OpenAPI-спецификации с https://jsonplaceholder.typicode.com
- [ ] Реализовать парсинг: извлечь список эндпоинтов (метод, путь, тело, коды ответов, схема ответа)
- [ ] Обработать ошибки: сервис недоступен, неожиданный формат спеки

## 4. Промпт и генерация (общее)

- [ ] Собрать промпт: описание API + задание (4 CRUD-теста / JSON-массив тестов)
- [ ] Реализовать отправку промпта в Ollama (HTTP API)
- [ ] Реализовать повторный запрос при некорректном ответе модели (до --max-retries)

---

## Simple: pytest-генератор

### 5. Simple — обработка ответа и запуск тестов

- [ ] Извлечь pytest-код из ответа модели (strip-маркировки / JSON-обёртки)
- [ ] Сохранить код в выходную папку как .py-файл
- [ ] Запустить pytest и собрать результат (количество зелёных/красных)
- [ ] Вывести отчёт: что сгенерировано, результат прогона

### 6. Simple — CLI и интеграция

- [ ] Реализовать CLI-точку входа (аргументы: --output-dir, --max-retries и т.д.)
- [ ] Связать все шаги в один пайплайн: спека → промпт → генерация → сохранение → pytest → отчёт

---

## Advanced: JSON + движок

### 7. Advanced — JSON-формат

- [ ] Определить формат JSON от LLM (эндпоинт + метод + данные + опционально ожидаемый ответ)
- [ ] Обновить промпт: требовать ответ строго в этом формате
- [ ] Реализовать парсинг JSON от модели

### 8. Advanced — движок тестов

- [ ] Реализовать HTTP-движок: принимает JSON-массив тестов, шлёт CRUD-запросы к API
- [ ] Определить: параллельно или последовательно (и обосновать)
- [ ] Валидация ответов: статус-код + тело (по Swagger и/или от LLM)
- [ ] Обработка ошибок: сеть недоступна, таймаут, невалидный JSON от LLM

### 9. Advanced — отчёт

- [ ] Формат отчёта: что прошло, что упало, детали (время, тело запроса/ответа)
- [ ] Экспорт отчёта (markdown / JSON / консоль)

### 10. Advanced — CLI и интеграция

- [ ] CLI-точка входа для advanced-варианта
- [ ] Пайплайн: спека → промпт → JSON → движок → отчёт
